import { Box } from "@mui/material";

export const ManageAccounts = () => {
  return (
    <Box
      sx={{
        textAlign: "center",
        background: "red",
        height: "200vh",
      }}
    ></Box>
  );
};
