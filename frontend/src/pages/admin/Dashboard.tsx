import { Box } from "@mui/material";
import React from "react";

const Dashboard = () => {
  return (
    <Box
      sx={{
        textAlign: "center",
        background: "black",
        height: "200vh",
      }}
    ></Box>
  );
};

export default Dashboard;
